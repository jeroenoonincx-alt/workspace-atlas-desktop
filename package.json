{
  "name": "workspace-atlas-desktop",
  "private": true,
  "version": "0.8.3",
  "description": "Workspace Atlas for Desktop Start — Tauri 2 Windows testbuild",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
